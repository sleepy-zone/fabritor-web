export default function ClipSetter (props) {  
  const { object } = props;

  const handleClip = () => {
    
  }

  return (
    <span className="fabritor-toolbar-setter-trigger" onClick={handleClip}>裁剪</span>
  )
}